{
  "name": "@open-pstack/poteto-mode-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "\"$npm_execpath\" test --parallel bootstrap orch watch-pr runner check-plan",
    "typecheck": "tsc --project watch-pr/tsconfig.json --noEmit --strict && tsc --project runner/tsconfig.json --noEmit --strict && tsc --project check-plan.tsconfig.json --noEmit --strict"
  },
  "dependencies": {
    "commander": "14.0.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "latest"
  }
}
